# Personal OS Bootstrap

This file has been restructured into `_bootstrap/` for maintainability.

**To run the bootstrap:** paste `_bootstrap/README.md` into Claude Code.
**To edit the bootstrap:** modify files in `_bootstrap/phases/`.
**After bootstrap:** `bash _bootstrap/archive.sh` to clean up root.
